import React from 'react'

const SelectBar = (props) => {

    const { colName, options } = props

    return(
        <select
            // id='major'
            // name='major'
            // onChange={handleChange}
            // value={formData.major}
            // className={errorBox.major ? 'errorStyle' : ''} 
        >

        </select>
    )
}

export default SelectBar